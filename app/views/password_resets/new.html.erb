<% provide(:title, "Nowe hasło") %>
<h1>Nowe hasło</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for(:password_reset, url: password_resets_path) do |f| %>
      <%= f.label :email %>
      <%= f.email_field :email, class: 'form-control' %>
      <p>Na podany adres zostanie wysłana wiadomość z linkiem do strony, na której można zmienić hasło.</p>
      <%= f.submit "Wyślij", class: "btn btn-primary" %>
    <% end %>
  </div>
</div>