$("#show-comments-btn<%= post.id %>").click(function () {

    var comments = $(this).closest(".content").find(".fb-comments-container");
    if (comments.css('display') == 'none'){

        comments.show();
    }
    else {
        comments.hide();
    };
});